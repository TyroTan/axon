# Track data — local only (raw responses, session data, context snapshots)
track_*/sessions/
track_*/context/*.snapshot.md

# Build artifacts
axon
*.out
web/dist/

# Frontend
ui/node_modules/
ui/.vite/

# Runtime data — local only
conversations/
metrics.jsonl

# Environment
.env
.env.*
